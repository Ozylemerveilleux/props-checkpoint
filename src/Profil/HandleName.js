const HandleName = props => {
    return(
        <button onClick={() => props.alertMyinput ('I am mister Ismaila Holdi COLY')}>
            ClickMe 
        </button>
    );
}

export default HandleName;